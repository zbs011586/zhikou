<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zhikou.code.dao.FansDao">

    <select id="getFansCount" parameterType="INTEGER" resultType="INTEGER">
        select count(*) from t_fans where concern_user_id=#{userId}
    </select>

    <select id="getConcernCount" parameterType="INTEGER" resultType="INTEGER">
        select count(*) from t_fans where fans_user_id=#{userId}
    </select>

    <select id="getFansList" parameterType="INTEGER" resultType="HashMap">
        select fans_user_id as userId,fans_nickname as nickName,fans_avatar as avatar
        from t_fans where concern_user_id=#{userId}
    </select>

    <select id="getConcernList" parameterType="INTEGER" resultType="HashMap">
        select concern_user_id as userId,concern_nickname as nickName,concern_avatar as avatar
        from t_fans where fans_user_id=#{userId}
    </select>

</mapper>