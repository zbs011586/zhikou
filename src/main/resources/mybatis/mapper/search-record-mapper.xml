<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zhikou.code.dao.SearchRecordDao">

    <select id="mySearch" parameterType="INTEGER" resultType="String">
        select input_text from t_search_record where user_id=#{userId}
            order by create_time desc limit 10
    </select>

    <select id="hotSearch" resultType="String">
        select input_text from t_search_record GROUP BY input_text ORDER BY COUNT(id) desc limit 10
    </select>

</mapper>